const btn = document.getElementById("mensagem-btn");
const texto = document.getElementById("mensagem-texto");

btn.addEventListener("click", () => {
    texto.textContent =
        "Nunca pare de aprender. Cada linha de código é um passo mais perto do seu sonho!";
});
